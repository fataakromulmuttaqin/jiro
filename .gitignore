venv/
__pycache__/
*.pyc
.pytest_cache/
.env
positions.json
ledger.json
seen_terms.json
*.log
# smart-money watchlist (real wallet addresses — never commit)
watchlist.json
# never commit the burner wallet
*.key
*wallet*.json
# ML model artifacts (regenerated by train)
models/
*.joblib
*.npz
ml_training_samples.jsonl
ml_last_train.txt

# Website build artifacts (regenerated by `npm run build`)
website/node_modules/
website/dist/
website/.tsbuildinfo

# Python caches inside website/ (none expected but just in case)
website/**/__pycache__/
top_traders/*/cache/
